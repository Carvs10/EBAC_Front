"use strict";
function calculaArea(base, altura) {
    return base * altura;
}
const areaCalc = (base, altura) => base * altura;
function sum(...numbers) {
    // numbers.reduce()
    console.log(numbers);
}
function teste() {
    if (10 > 5) {
        return "Dez eh maior que cinco";
    }
    else {
        return 5;
    }
}
