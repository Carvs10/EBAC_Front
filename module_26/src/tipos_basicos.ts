//Data types
let isRaining: boolean = false;

isRaining = true;

let age: number = 27;
let height: number = 1.75;

const nationality: string = "brazilian";

//Arrays
const patterns: string[] = ["lucas", "fernanda", "joao", "miguel", "marie"];

const technologies: Array<string> = ["html", "css", "js", "ts"];

const grades: ReadonlyArray<number> = [7, 9, 5, 8];

// Tuples

const list: [name: string, isStudying: boolean, number] = ["joao", true, 23];

//union type
let anaAge: number | string = 25;

let apiData: any;
